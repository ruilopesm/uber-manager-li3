#ifndef USERS_PAGER_H
#define USERS_PAGER_H

#include "base/interactive.h"

/**
 * @brief Function that draws the users pager component to a window
 *
 * @param manager - The manager of type `MANAGER`
 */
void users_pager(MANAGER manager);

#endif
